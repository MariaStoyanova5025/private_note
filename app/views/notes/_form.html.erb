<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :msg %>
    <%= form.text_field :msg, id: :note_msg %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :note_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
